/**
 * @fileoverview Type exports for @tbk/auth
 * @module @tbk/auth/types
 */

// Auth types (roles, permissions, interfaces)
export * from './auth';

// Database types (Supabase schema)
export * from './database';
