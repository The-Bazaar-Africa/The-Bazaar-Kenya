# =============================================================================
# THE BAZAAR — Main App Environment Variables (EXAMPLE)
# =============================================================================
# Copy this file to `.env.local` and fill in real values.
# NEVER commit `.env.local` to version control.
# =============================================================================


# -----------------------------------------------------------------------------
# Application Runtime
# -----------------------------------------------------------------------------
NODE_ENV=development
NEXT_PUBLIC_APP_NAME=The Bazaar
NEXT_PUBLIC_APP_URL=http://localhost:3000


# -----------------------------------------------------------------------------
# Backend API URL
# -----------------------------------------------------------------------------
# All API calls go through the backend-api (ADR-001)
NEXT_PUBLIC_API_URL=http://localhost:4000

# Production:
# NEXT_PUBLIC_API_URL=https://api.thebazaar.co.ke


# -----------------------------------------------------------------------------
# Supabase (Auth Only — Read ADR-001)
# -----------------------------------------------------------------------------
# These are PUBLIC keys for client-side auth initialization ONLY.
# All data access MUST go through backend-api, NOT directly to Supabase.
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# SECURITY:
# - These are PUBLIC keys — safe to expose in browser
# - Used ONLY for Supabase Auth (login, signup, session)
# - DO NOT use for direct database queries (use backend-api instead)


# -----------------------------------------------------------------------------
# Paystack (Public Key Only — ADR-002)
# -----------------------------------------------------------------------------
# Get from: https://dashboard.paystack.com/#/settings/developer
NEXT_PUBLIC_PAYSTACK_PUBLIC_KEY=pk_test_xxxxxxxxxxxxxxxxx

# SECURITY (CRITICAL):
# - This is the PUBLIC key — safe to expose in browser
# - Used for Paystack Popup/Inline payment initialization
# - DO NOT add PAYSTACK_SECRET_KEY here — it belongs in backend-api ONLY
# - DO NOT add PAYSTACK_WEBHOOK_SECRET here — it belongs in backend-api ONLY
#
# Payment Flow:
# 1. Frontend calls backend-api to initiate payment
# 2. Backend-api returns authorization_url or access_code
# 3. Frontend uses public key to render Paystack Popup
# 4. User completes payment on Paystack
# 5. Paystack sends webhook to backend-api
# 6. Backend-api updates order status


# -----------------------------------------------------------------------------
# Analytics (Optional)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_HOTJAR_ID=
